//This file is used just to simplify exporting. app vitra ko page.tsx uses this. Whenever @component folder bata anything is imported , it refers to the index.ts inside it.
import Hero from "./Hero";
import CustomButton from "./CustomButton";
import NavBar from "./Navbar";
import Footer from "./Footer";

export { Hero, CustomButton, Footer, NavBar };
